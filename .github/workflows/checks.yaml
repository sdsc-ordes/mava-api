name: SHACL validation

on:
  workflow_call:
  push:
    paths:
      - "src/ontology/**"
  pull_request:
    paths:
      - "src/ontology/**"
jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: SHACL validation
        run: |
          just ci just validate-ontology
